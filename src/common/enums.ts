export enum MessageType {
  FLOW = 'FLOW',
  ALERT = 'ALERT',
}

export enum Sentiment {
  BULLISH = 'BULLISH',
  BEARISH = 'BEARISH',
}

export enum Position {
  CALL = 'CALL',
  PUT = 'PUT',
}

export enum Type {
  SWEEP = 'SWEEP',
}
